def versionMajor = 0
def versionMinor = 0
def versionBuild = 1

def build_versions = [:]
build_versions.versionCode = versionMajor * 10000 + versionMinor * 1000 + versionBuild
build_versions.versionName = "${versionMajor}.${versionMinor}.${versionBuild}"
build_versions.min_sdk = 16
build_versions.target_sdk = 28
ext.build_versions = build_versions
